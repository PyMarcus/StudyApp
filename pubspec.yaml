name: study_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.7.2


dependencies:
  flutter:
    sdk: flutter
  
  # para localizacao
  get_it: ^8.0.3
  # para controle de estado
  flutter_bloc: ^9.1.1
  # para igualdade de valores
  equatable: ^2.0.7
  # para recursos de programacao funcional
  dartz: ^0.10.1
  # uso de api
  http: ^1.4.0
  # localstorage
  shared_preferences: ^2.5.3

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.6


  flutter_lints: ^5.0.0
  
flutter:


  uses-material-design: true

 